numpy==1.26.4
PyQt5==5.15.10
pyserial==3.5
pygame>=2.5.0  # Para simulação do joystick
scipy==1.13.1
shapely==2.0.4
RPi.GPIO>=0.7.1 # Para controle de hardware na Raspberry Pi
pyqtgraph==0.13.3

# Dependências para processamento de mapas Aurora → C1
open3d>=0.18.0  # Para processamento de nuvens de pontos 3D
Pillow>=10.0.0  # Para manipulação de imagens (PGM)
PyYAML>=6.0  # Para arquivos YAML de configuração de mapas
requests>=2.31.0  # Para comunicação HTTP com API SLAMWARE do C1

# RP Lidar C1 (Fase 2 - detecção de obstáculos)
# rplidarc1: específico para C1 (evita "Descriptor length mismatch" do rplidar genérico)
rplidarc1>=0.1.3
# pyrplidarsdk: SDK oficial SLAMTEC (backend principal desde Mar 2026 — rplidarc1 inconsistente)
pyrplidarsdk>=0.1.2
matplotlib>=3.5.0  # Para tools/c1_mapa_visual.py (mapeamento gráfico do C1)

# IMU BNO08x (Raspberry Pi apenas - para tools/bno08x_test.py)
adafruit-blinka>=8.0.0
adafruit-circuitpython-bno08x>=2.0.0
# Opcional: se I2C falhar com "No Hardware I2C", use ExtendedI2C(1) para /dev/i2c-1
adafruit-extended-bus>=1.0.0